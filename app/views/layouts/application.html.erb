<!DOCTYPE html>
<html>
  <head>
    <title>BudgetApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <section class="page-wrapper">
    <nav class="main-nav">
          <ul class="navbar-links">
              <li><%= link_to 'Home', root_path %></li>
              <li><%= link_to 'Categories', groups_path %></li>
          </ul>
          <div class="navbar-btns">
              <% if user_signed_in? %>
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "signout-btn" %>
              <% else %>
                <%= button_to "Login", new_user_session_path, class: "login-btn" %>
              <% end %>
          </div>
  </nav>
 
    <%= yield %>
    </section>
    <footer>
    <p>&copy; <%= Time.now.year %> BudgetApp by Abdel-Khafid SALAOU. All rights reserved.</p>
  </footer>
  </body>
</html>
